# Simple Note

This note doesn't have frontmatter, so the title will be extracted from the H1 heading.

It has an inline #example tag, so it should still match filters that look for the example tag.

## Section

Some content here.

